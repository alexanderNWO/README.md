name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "5 * * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: michaelcontento/credly-import-action@v1.0.1
        with:       
          name: alexandernwo
